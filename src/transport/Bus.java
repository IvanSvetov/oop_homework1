package transport;

public class Bus extends Transport {

    public Bus(String brand, String model, int year, String country, String colour, String maxSpeed) {
        super(brand, model, year, country, colour, maxSpeed);
    }
}
